// examples/demo.ltl — runnable demo for ltl_dna_stats

import std::io
use ltl_dna_stats as lib

fn main() {
    io::println("ltl_dna_stats demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}