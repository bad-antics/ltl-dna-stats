// ltl-dna-stats/main.ltl
//
// GC content / codon / reverse-complement utilities.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dna-stats v0.1.0")
    io::println("GC content / codon / reverse-complement utilities.")
    0
}
